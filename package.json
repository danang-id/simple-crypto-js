{
  "name": "simple-crypto-js",
  "version": "2.0.0",
  "description": "Simplify AES encryption and decryption of any JavaScript objects, implementing crypto-js library.",
  "main": "src/SimpleCrypto.js",
  "homepage": "https://github.com/danang-id/simple-crypto-js",
  "scripts": {
    "build": "gulp scripts",
    "test": "mocha --reporter spec --require ts-node/register 'test/**/*.test.ts'",
    "test:watch": "mocha --watch --reporter spec --require ts-node/register 'test/**/*.test.ts'",
    "coverage": "mocha --reporter mocha-lcov-reporter --require ts-node/register 'test/**/*.test.ts' | coveralls"
  },
  "dependencies": {
    "blanket": "^1.2.3",
    "crypto-js": "^3.1.9-1"
  },
  "author": "https://github.com/danang-id",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.1.3",
    "@types/crypto-js": "^3.1.39",
    "@types/mocha": "^5.2.0",
    "chai": "^4.1.2",
    "coveralls": "^3.0.1",
    "gulp": "^3.9.1",
    "gulp-typescript": "^4.0.2",
    "mocha": "^5.2.0",
    "mocha-lcov-reporter": "^1.3.0",
    "ts-node": "^6.0.5",
    "typescript": "^2.8.3"
  },
  "config": {
    "blanket": {
      "pattern": [],
      "data-cover-never": [
        "node_modules",
        "tests"
      ]
    }
  }
}
